let nombre = 'Straw Hat';
let real = 'Monkey D Luffy';

console.log(`Nombre ${nombre} real ${real}`);

const getName = () => {
    return `${nombre} ${real}`;
}

console.log(`El nombre de ${getName()}`);